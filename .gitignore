### === Environment / secrets ===
.env
.env.local
.env.*.local
*.env
!.env.example

### === Java / Spring Boot (backend-spring) ===
backend-spring/target/
# NOTE: mvnw, mvnw.cmd and .mvn/ are deliberately NOT ignored — the Maven
# wrapper is committed so a fresh clone (and CI) can build with ./mvnw
# without installing Maven. It is configured as distributionType=only-script,
# so no maven-wrapper.jar is committed either.
*.class
*.jar
*.war
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

### === Node.js (backend-node, frontend) ===
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
dist/
dist-ssr/
*.local
.eslintcache

### === Python (ml-hotspots, Phase 7 only) ===
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
env/
.ipynb_checkpoints/

### === Databases / local dev ===
*.sqlite3
*.db
pgdata/
postgres-data/

### === IDE / editor ===
.vscode/*
!.vscode/extensions.json
.idea/
*.iml
*.swp
*.swo

### === OS ===
.DS_Store
Thumbs.db
desktop.ini

### === Logs ===
logs/
*.log

### === Misc build artifacts ===
coverage/
*.tsbuildinfo
